[{"authorTime":"2018-08-26 22:43:11","codes":[{"authorDate":"2018-08-26 22:43:11","commitOrder":1,"curCode":"    void setTasks( PushTaskScanner scanner, FileObject resource, List<? extends Task> tasks, TaskFilter filter ) throws IOException {\n        lock.writeLock().lock();\n        \n        List<Task> removed = clear( scanner, resource );\n        \n        Set<Task> tasksToAdd = null;\n        int currentCount = countTasks( scanner );\n        for( Task t : tasks ) {\n            if( filter.accept( t ) && !filter.isTaskCountLimitReached(currentCount) ) {\n                currentCount++;\n                \n                if( sortedTasks.contains( t ) || (tasksToAdd != null && tasksToAdd.contains( t ) ) )\n                    continue;\n                \n                if( null == tasksToAdd )\n                    tasksToAdd = new HashSet<Task>( tasks.size() );\n                \n                List<Task> scannerTasks = pushScanner2tasks.get( scanner );\n                if( null == scannerTasks ) {\n                    scannerTasks = new ArrayList<Task>();\n                    pushScanner2tasks.put( scanner, scannerTasks );\n                }\n                \n                TaskGroup group = Accessor.getGroup( t );\n                List<Task> groupTasks = group2tasks.get( group );\n                if( null == groupTasks ) {\n                    groupTasks = new ArrayList<Task>();\n                    group2tasks.put( group, groupTasks );\n                }\n\n                tasksToAdd.add( t );\n                scannerTasks.add( t );\n                groupTasks.add( t );\n            }\n        }\n        if( null != tasksToAdd ) {\n            addTasks( tasksToAdd );\n        }\n        \n        lock.writeLock().unlock();\n        \n        if( null != removed && !removed.isEmpty() )\n            fireTasksRemoved( removed );\n        if( null != tasksToAdd && !tasksToAdd.isEmpty() )\n            fireTasksAdded(new ArrayList(tasksToAdd));\n    }\n","date":"2018-08-26 22:43:11","endLine":104,"groupId":"46585","id":1,"instanceNumber":1,"isCurCommit":0,"methodName":"setTasks","params":"(PushTaskScannerscanner@FileObjectresource@List<?extendsTask>tasks@TaskFilterfilter)","path":"/mnt/clonedata/CloneManagementServer/ManagementServer/consistResult/result-netbeans-10-0.7/blobInfo/CC_OUT/blobs/40/4832da6a9fd843210cb948456b5b2850b3442b.src","preCode":"    void setTasks( PushTaskScanner scanner, FileObject resource, List<? extends Task> tasks, TaskFilter filter ) throws IOException {\n        lock.writeLock().lock();\n        \n        List<Task> removed = clear( scanner, resource );\n        \n        Set<Task> tasksToAdd = null;\n        int currentCount = countTasks( scanner );\n        for( Task t : tasks ) {\n            if( filter.accept( t ) && !filter.isTaskCountLimitReached(currentCount) ) {\n                currentCount++;\n                \n                if( sortedTasks.contains( t ) || (tasksToAdd != null && tasksToAdd.contains( t ) ) )\n                    continue;\n                \n                if( null == tasksToAdd )\n                    tasksToAdd = new HashSet<Task>( tasks.size() );\n                \n                List<Task> scannerTasks = pushScanner2tasks.get( scanner );\n                if( null == scannerTasks ) {\n                    scannerTasks = new ArrayList<Task>();\n                    pushScanner2tasks.put( scanner, scannerTasks );\n                }\n                \n                TaskGroup group = Accessor.getGroup( t );\n                List<Task> groupTasks = group2tasks.get( group );\n                if( null == groupTasks ) {\n                    groupTasks = new ArrayList<Task>();\n                    group2tasks.put( group, groupTasks );\n                }\n\n                tasksToAdd.add( t );\n                scannerTasks.add( t );\n                groupTasks.add( t );\n            }\n        }\n        if( null != tasksToAdd ) {\n            addTasks( tasksToAdd );\n        }\n        \n        lock.writeLock().unlock();\n        \n        if( null != removed && !removed.isEmpty() )\n            fireTasksRemoved( removed );\n        if( null != tasksToAdd && !tasksToAdd.isEmpty() )\n            fireTasksAdded(new ArrayList(tasksToAdd));\n    }\n","realPath":"ide/tasklist.ui/src/org/netbeans/modules/tasklist/impl/TaskList.java","repoName":"netbeans","snippetEndLine":0,"snippetStartLine":0,"startLine":59,"status":"B"},{"authorDate":"2018-08-26 22:43:11","commitOrder":1,"curCode":"    void update( FileTaskScanner scanner, FileObject resource, List<Task> newTasks, TaskFilter filter ) {\n        lock.writeLock().lock();\n        \n        List<Task> removed = clear( scanner, resource );\n\n        Set<Task> tasksToAdd = new HashSet<Task>(newTasks.size());\n        for( Task t : newTasks ) {\n            if( sortedTasks.contains( t ) || tasksToAdd.contains( t ) )\n                continue;\n            if( !filter.isTaskCountLimitReached( countTasks( scanner ) ) && filter.accept( t ) ) {\n                List<Task> scannerTasks = fileScanner2tasks.get( scanner );\n                if( null == scannerTasks ) {\n                    scannerTasks = new ArrayList<Task>();\n                    fileScanner2tasks.put( scanner, scannerTasks );\n                }\n                TaskGroup group = Accessor.getGroup( t );\n                List<Task> groupTasks = group2tasks.get( group );\n                if( null == groupTasks ) {\n                    groupTasks = new ArrayList<Task>();\n                    group2tasks.put( group, groupTasks );\n                }\n                scannerTasks.add( t );\n                groupTasks.add( t );\n                tasksToAdd.add( t );\n            }\n        }\n        if( !tasksToAdd.isEmpty() ) {\n            addTasks( tasksToAdd );\n        }\n\n        lock.writeLock().unlock();\n\n        if( null != removed && !removed.isEmpty() )\n            fireTasksRemoved( removed );\n        if( !tasksToAdd.isEmpty() )\n            fireTasksAdded(new ArrayList<Task>(tasksToAdd));\n    }\n","date":"2018-08-26 22:43:11","endLine":192,"groupId":"41846","id":2,"instanceNumber":2,"isCurCommit":0,"methodName":"update","params":"(FileTaskScannerscanner@FileObjectresource@List<Task>newTasks@TaskFilterfilter)","path":"/mnt/clonedata/CloneManagementServer/ManagementServer/consistResult/result-netbeans-10-0.7/blobInfo/CC_OUT/blobs/40/4832da6a9fd843210cb948456b5b2850b3442b.src","preCode":"    void update( FileTaskScanner scanner, FileObject resource, List<Task> newTasks, TaskFilter filter ) {\n        lock.writeLock().lock();\n        \n        List<Task> removed = clear( scanner, resource );\n\n        Set<Task> tasksToAdd = new HashSet<Task>(newTasks.size());\n        for( Task t : newTasks ) {\n            if( sortedTasks.contains( t ) || tasksToAdd.contains( t ) )\n                continue;\n            if( !filter.isTaskCountLimitReached( countTasks( scanner ) ) && filter.accept( t ) ) {\n                List<Task> scannerTasks = fileScanner2tasks.get( scanner );\n                if( null == scannerTasks ) {\n                    scannerTasks = new ArrayList<Task>();\n                    fileScanner2tasks.put( scanner, scannerTasks );\n                }\n                TaskGroup group = Accessor.getGroup( t );\n                List<Task> groupTasks = group2tasks.get( group );\n                if( null == groupTasks ) {\n                    groupTasks = new ArrayList<Task>();\n                    group2tasks.put( group, groupTasks );\n                }\n                scannerTasks.add( t );\n                groupTasks.add( t );\n                tasksToAdd.add( t );\n            }\n        }\n        if( !tasksToAdd.isEmpty() ) {\n            addTasks( tasksToAdd );\n        }\n\n        lock.writeLock().unlock();\n\n        if( null != removed && !removed.isEmpty() )\n            fireTasksRemoved( removed );\n        if( !tasksToAdd.isEmpty() )\n            fireTasksAdded(new ArrayList<Task>(tasksToAdd));\n    }\n","realPath":"ide/tasklist.ui/src/org/netbeans/modules/tasklist/impl/TaskList.java","repoName":"netbeans","snippetEndLine":0,"snippetStartLine":0,"startLine":156,"status":"B"}],"commitId":"0f2e98d513deaedccef831ee9cfc08d09f3650b6","commitMessage":"@@@Merge pull request #791 from ralphbenjamin/NETBEANS-1172_move_ide_modules_to_ide_subdirectory\n\n[NETBEANS-1172] move ide modules to ide subdirectory","date":"2018-08-26 22:43:11","modifiedFileCount":"0","status":"B","submitter":"Geertjan Wielenga"},{"authorTime":"2018-08-26 22:43:11","codes":[{"authorDate":"2019-11-15 03:36:52","commitOrder":2,"curCode":"    void setTasks( PushTaskScanner scanner, FileObject resource, List<? extends Task> tasks, TaskFilter filter ) throws IOException {\n        lock.writeLock().lock();\n        \n        List<Task> removed = clear( scanner, resource );\n        \n        Set<Task> tasksToAdd = null;\n        int currentCount = countTasks( scanner );\n        for( Task t : tasks ) {\n            if( filter.accept( t ) && !filter.isTaskCountLimitReached(currentCount) ) {\n                currentCount++;\n                \n                if( sortedTasks.contains( t ) || (tasksToAdd != null && tasksToAdd.contains( t ) ) )\n                    continue;\n                \n                if( null == tasksToAdd )\n                    tasksToAdd = new HashSet<Task>( tasks.size() );\n                \n                List<Task> scannerTasks = pushScanner2tasks.get( scanner );\n                if( null == scannerTasks ) {\n                    scannerTasks = new ArrayList<Task>();\n                    pushScanner2tasks.put( scanner, scannerTasks );\n                }\n                \n                TaskGroup group = Accessor.getGroup( t );\n                List<Task> groupTasks = group2tasks.get( group );\n                if( null == groupTasks ) {\n                    groupTasks = new ArrayList<Task>();\n                    group2tasks.put( group, groupTasks );\n                }\n\n                tasksToAdd.add( t );\n                scannerTasks.add( t );\n                groupTasks.add( t );\n            }\n        }\n        if( null != tasksToAdd ) {\n            addTasks( tasksToAdd );\n        }\n        \n        lock.writeLock().unlock();\n        \n        if( null != removed && !removed.isEmpty() )\n            fireTasksRemoved( removed );\n        if( null != tasksToAdd && !tasksToAdd.isEmpty() )\n            fireTasksAdded(new ArrayList<Task>(tasksToAdd));\n    }\n","date":"2019-11-21 09:29:41","endLine":104,"groupId":"101867","id":3,"instanceNumber":1,"isCurCommit":0,"methodName":"setTasks","params":"(PushTaskScannerscanner@FileObjectresource@List<?extendsTask>tasks@TaskFilterfilter)","path":"/mnt/clonedata/CloneManagementServer/ManagementServer/consistResult/result-netbeans-10-0.7/blobInfo/CC_OUT/blobs/0a/a6352185aaf1fadf81801bebead5d2fe19b3c1.src","preCode":"    void setTasks( PushTaskScanner scanner, FileObject resource, List<? extends Task> tasks, TaskFilter filter ) throws IOException {\n        lock.writeLock().lock();\n        \n        List<Task> removed = clear( scanner, resource );\n        \n        Set<Task> tasksToAdd = null;\n        int currentCount = countTasks( scanner );\n        for( Task t : tasks ) {\n            if( filter.accept( t ) && !filter.isTaskCountLimitReached(currentCount) ) {\n                currentCount++;\n                \n                if( sortedTasks.contains( t ) || (tasksToAdd != null && tasksToAdd.contains( t ) ) )\n                    continue;\n                \n                if( null == tasksToAdd )\n                    tasksToAdd = new HashSet<Task>( tasks.size() );\n                \n                List<Task> scannerTasks = pushScanner2tasks.get( scanner );\n                if( null == scannerTasks ) {\n                    scannerTasks = new ArrayList<Task>();\n                    pushScanner2tasks.put( scanner, scannerTasks );\n                }\n                \n                TaskGroup group = Accessor.getGroup( t );\n                List<Task> groupTasks = group2tasks.get( group );\n                if( null == groupTasks ) {\n                    groupTasks = new ArrayList<Task>();\n                    group2tasks.put( group, groupTasks );\n                }\n\n                tasksToAdd.add( t );\n                scannerTasks.add( t );\n                groupTasks.add( t );\n            }\n        }\n        if( null != tasksToAdd ) {\n            addTasks( tasksToAdd );\n        }\n        \n        lock.writeLock().unlock();\n        \n        if( null != removed && !removed.isEmpty() )\n            fireTasksRemoved( removed );\n        if( null != tasksToAdd && !tasksToAdd.isEmpty() )\n            fireTasksAdded(new ArrayList(tasksToAdd));\n    }\n","realPath":"ide/tasklist.ui/src/org/netbeans/modules/tasklist/impl/TaskList.java","repoName":"netbeans","snippetEndLine":0,"snippetStartLine":0,"startLine":59,"status":"M"},{"authorDate":"2018-08-26 22:43:11","commitOrder":2,"curCode":"    void update( FileTaskScanner scanner, FileObject resource, List<Task> newTasks, TaskFilter filter ) {\n        lock.writeLock().lock();\n        \n        List<Task> removed = clear( scanner, resource );\n\n        Set<Task> tasksToAdd = new HashSet<Task>(newTasks.size());\n        for( Task t : newTasks ) {\n            if( sortedTasks.contains( t ) || tasksToAdd.contains( t ) )\n                continue;\n            if( !filter.isTaskCountLimitReached( countTasks( scanner ) ) && filter.accept( t ) ) {\n                List<Task> scannerTasks = fileScanner2tasks.get( scanner );\n                if( null == scannerTasks ) {\n                    scannerTasks = new ArrayList<Task>();\n                    fileScanner2tasks.put( scanner, scannerTasks );\n                }\n                TaskGroup group = Accessor.getGroup( t );\n                List<Task> groupTasks = group2tasks.get( group );\n                if( null == groupTasks ) {\n                    groupTasks = new ArrayList<Task>();\n                    group2tasks.put( group, groupTasks );\n                }\n                scannerTasks.add( t );\n                groupTasks.add( t );\n                tasksToAdd.add( t );\n            }\n        }\n        if( !tasksToAdd.isEmpty() ) {\n            addTasks( tasksToAdd );\n        }\n\n        lock.writeLock().unlock();\n\n        if( null != removed && !removed.isEmpty() )\n            fireTasksRemoved( removed );\n        if( !tasksToAdd.isEmpty() )\n            fireTasksAdded(new ArrayList<Task>(tasksToAdd));\n    }\n","date":"2018-08-26 22:43:11","endLine":192,"groupId":"101867","id":4,"instanceNumber":2,"isCurCommit":0,"methodName":"update","params":"(FileTaskScannerscanner@FileObjectresource@List<Task>newTasks@TaskFilterfilter)","path":"/mnt/clonedata/CloneManagementServer/ManagementServer/consistResult/result-netbeans-10-0.7/blobInfo/CC_OUT/blobs/40/4832da6a9fd843210cb948456b5b2850b3442b.src","preCode":"    void update( FileTaskScanner scanner, FileObject resource, List<Task> newTasks, TaskFilter filter ) {\n        lock.writeLock().lock();\n        \n        List<Task> removed = clear( scanner, resource );\n\n        Set<Task> tasksToAdd = new HashSet<Task>(newTasks.size());\n        for( Task t : newTasks ) {\n            if( sortedTasks.contains( t ) || tasksToAdd.contains( t ) )\n                continue;\n            if( !filter.isTaskCountLimitReached( countTasks( scanner ) ) && filter.accept( t ) ) {\n                List<Task> scannerTasks = fileScanner2tasks.get( scanner );\n                if( null == scannerTasks ) {\n                    scannerTasks = new ArrayList<Task>();\n                    fileScanner2tasks.put( scanner, scannerTasks );\n                }\n                TaskGroup group = Accessor.getGroup( t );\n                List<Task> groupTasks = group2tasks.get( group );\n                if( null == groupTasks ) {\n                    groupTasks = new ArrayList<Task>();\n                    group2tasks.put( group, groupTasks );\n                }\n                scannerTasks.add( t );\n                groupTasks.add( t );\n                tasksToAdd.add( t );\n            }\n        }\n        if( !tasksToAdd.isEmpty() ) {\n            addTasks( tasksToAdd );\n        }\n\n        lock.writeLock().unlock();\n\n        if( null != removed && !removed.isEmpty() )\n            fireTasksRemoved( removed );\n        if( !tasksToAdd.isEmpty() )\n            fireTasksAdded(new ArrayList<Task>(tasksToAdd));\n    }\n","realPath":"ide/tasklist.ui/src/org/netbeans/modules/tasklist/impl/TaskList.java","repoName":"netbeans","snippetEndLine":0,"snippetStartLine":0,"startLine":156,"status":"N"}],"commitId":"49317c29e503d4bbf282521cf472814b2d37b91b","commitMessage":"@@@[NETBEANS-3393] - cleanup ArrayList uncheck conversion\n\nThere are many places in the code where an unchecked conversion warning is printed by the compiler..\n\n   [repeat] /home/bwalker/src/netbeans/profiler/profiler.attach/src/org/netbeans/modules/profiler/attach/dialog/AttachDialog.java:146: warning: [unchecked] unchecked conversion\n   [repeat]             List<RunningVM> targets = new ArrayList();\n   [repeat]                                       ^\n   [repeat]   required: List<RunningVM>\n   [repeat]   found:    ArrayList\n\nI've tried to remove these but there are several places where it still exists. This is because\nit would require work other than being simple to fix. I'll try to fix those later.\n\nupdates..\n","date":"2019-11-21 09:29:41","modifiedFileCount":"33","status":"M","submitter":"Brad Walker"}]
